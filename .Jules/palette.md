## 2025-01-26 - Navigation Embedded in Content
**Learning:** The site's navigation is embedded directly in the content file (`index.md`) rather than the layout file (`_layouts/default.html`).
**Action:** Skip-link targets (like `#main-content`) must be placed within `index.md` (specifically on the `.hero-section`) to effectively bypass the navigation.
